name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: 
      labels: dharmasvr2

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make -f makefile_azure.mak
